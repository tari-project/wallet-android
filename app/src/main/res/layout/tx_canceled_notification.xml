<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/notification_tx_received_vw_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="10dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@color/transparent"
                android:layout_gravity="center_vertical"
                android:textSize="13sp"
                android:text="@string/notification_tx_canceled_title"
                android:textColor="@color/home_tx_message"/>

            <TextView
                android:id="@+id/notification_tx_canceled_contact_alias_text_view"
                style="@style/TextAppearance.Compat.Notification.Title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/transparent"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:singleLine="true"
                android:text="@string/dummy_contact_alias"
                android:textColor="@color/home_tx_contact_alias"
                android:textSize="13sp"
                android:visibility="gone"/>

            <include
                android:id="@+id/notification_tx_canceled_emoji_summary_view"
                layout="@layout/emoji_id_summary"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />
        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:background="@color/transparent"
            android:ellipsize="end"
            android:text="@string/notification_tx_canceled_description"
            android:textColor="@color/home_tx_message"
            android:textSize="12sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/notification_tx_canceled_amount_text_view"
        android:layout_width="wrap_content"
        android:layout_height="23dp"
        android:background="@drawable/home_tx_value_canceled_bg"
        android:gravity="center"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:textColor="@color/home_tx_value_canceled"
        android:textSize="12sp"
        android:visibility="gone"
        tools:text="50.5" />

</LinearLayout>
